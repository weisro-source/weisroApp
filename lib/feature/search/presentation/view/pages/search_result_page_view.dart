import 'package:flutter/material.dart';

import 'search_result_page_view_body.dart';

class SearchResultPageView extends StatelessWidget {
  const SearchResultPageView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SearchResultPageViewBody());
  }
}
