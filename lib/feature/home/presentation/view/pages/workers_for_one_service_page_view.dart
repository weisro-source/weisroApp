import 'package:flutter/material.dart';

import 'workers_for_one_service_page_view_body.dart';

class WorkersForOneServicePageView extends StatelessWidget {
  const WorkersForOneServicePageView({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: WorkersForOneServicePageViewBody());
  }
}
