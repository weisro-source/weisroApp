import 'package:flutter/material.dart';

import 'worker_and_services_page_view_body.dart';

class WorkerAndServicesPageView extends StatelessWidget {
  const WorkerAndServicesPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: WorkerAndServicesPageViewBody(),
    );
  }
}
